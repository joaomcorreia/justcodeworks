// src/app/[locale]/sites/[slug]/page.tsx
// [ROUTES] Deprecated locale-based tenant route.
// Tenant sites now live at /sites/[slug] without locale.
// This stub exists only to avoid build errors.
// Do not remove or change the [locale] routing for JCW itself.

export default function LocaleTenantSitePage() {
  return null;
}